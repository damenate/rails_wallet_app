<%= form_for(@dealing) do |f| %>
  <% if @dealing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dealing.errors.count, "error") %> prohibited this dealing from being saved:</h2>

      <ul>
      <% @dealing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :receiver %><br>
    <%= f.text_field :receiver %>
  </div>
  <div class="field">
    <%= f.label :deal_type %><br>
    <%= f.text_field :deal_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
